#include <iostream>
#include <stack>
#include <vector>

using namespace std;

void move(int, vector<int>, vector<int>, vector<int>);
void display(int, vector<int>, vector<int>, vector<int>);

int main()
{
    int discNum;
    
    std::cout << "Welcome to Towers of Hanoi 2017, please input an integer between 1 and 100: ";
    do {
        std::cin >> discNum;
        if (discNum < 1 || discNum > 100) {
            std::cout << "Invalid input. ";
        }
    } while(discNum < 1 || discNum > 100);
    
    vector<char> a(discNum), b(discNum), c(discNum);
    
    for (int i = 1; i < discNum; i++) a.at(i) = i;
    for (int i = 1; i < discNum; i++) {
        b.at(i) = '_';
        c.at(i) = '_';
    }
    
    
    
    //move(discNum, a, b, c);

    return 0;
}

void move(int n, vector<int> a, vector<int> b, vector<int> c) {
    
}

void display(int, vector<int> a, vector<int> b, vector<int> c) {
    
}
